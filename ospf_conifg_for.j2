{%- if loopback0_addr is defined %}
interface Loopback0
 ip address {{loopback0_addr}} {{loopback0_mask}}
{%- endif%}
router ospf {{process_id }}
{%- for net in ospf_networks%}
 network {{net.network}} {{net.wildcard}} area {{net.area}}
{%- endfor %}